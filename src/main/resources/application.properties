spring.application.name=pos-service

# Tomcat access log is disabled by default because HTTP completion is
# logged as structured JSON. The debug profile enables timing details.
server.tomcat.accesslog.enabled=false
server.tomcat.accesslog.directory=logs
server.tomcat.accesslog.prefix=access
server.tomcat.accesslog.suffix=.log
server.tomcat.accesslog.buffered=false
# %t request time | %a remote ip | "%r" request line | %s status | %b bytes sent
server.tomcat.accesslog.pattern=%t %a "%r" %s %b reqId=%{X-Request-Id}o
server.compression.enabled=true
server.compression.mime-types=application/json,application/*+json,text/plain,text/html,text/xml,text/css,application/javascript
server.compression.min-response-size=1024
spring.datasource.url=jdbc:postgresql://157.10.161.207:5432/nivora-pos-db
spring.datasource.username=postgres
spring.datasource.password=ShaninHanan23
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Jakarta
spring.jpa.properties.hibernate.jdbc.batch_size=30
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true
spring.jpa.properties.hibernate.query.in_clause_parameter_padding=true
spring.jpa.properties.hibernate.query.plan_cache_max_size=2048
spring.jpa.show-sql=false
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1800000
spring.jackson.time-zone=Asia/Jakarta
jwt.secret=NivoraPOSSecretKey2024VeryLongSecretForHS256Algorithm
jwt.expiration=86400000
server.servlet.context-path=/NivoraPos
security.preauthorize.enabled=false
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
app.upload.max-size-bytes=5242880
app.image-upload.api-secret=
app.image-upload.signature-skew-seconds=300
app.storage.minio.endpoint=
app.storage.minio.access-key=
app.storage.minio.secret-key=
app.storage.minio.bucket=nivora-pos-images
app.storage.minio.object-prefix=images/product
app.storage.minio.public-base-url=
app.request-log.body-enabled=false
app.request-log.error-body-enabled=true
app.request-log.max-body-bytes=2048
app.psgs-token-cache.enabled=true
app.psgs-token-cache.max-ttl-seconds=3600
app.psgs-token-cache.cleanup-enabled=true
app.psgs-token-cache.cleanup-fixed-delay-ms=60000
psgs.integration.enabled=true
psgs.datasource.url=jdbc:mysql://localhost:3306/midware_master?useLegacyDatetimeCode=false&serverTimezone=Asia/Jakarta
psgs.datasource.username=root
psgs.datasource.password=123456
psgs.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
psgs.datasource.master-schema=midware_master
psgs.login.require-password=false

# ---------------------------------------------------------------
# Debug / performance logging — uncomment to enable in-place,
# OR run with --spring.profiles.active=debug (or prod,debug).
# Adds per-request "Stmts | Queries | Slowest query" line in the
# response log, plus logs any single query slower than the
# threshold (ms). Off by default; zero prod overhead.
# ---------------------------------------------------------------
#spring.jpa.properties.hibernate.generate_statistics=true
#logging.level.org.hibernate.stats=DEBUG
#spring.jpa.properties.hibernate.session.events.log.LOG_QUERIES_SLOWER_THAN_MS=50
#logging.level.org.hibernate.SQL_SLOW=INFO
#app.psgs-jdbc-log.enabled=true
#app.psgs-jdbc-log.slow-threshold-ms=50
# Very noisy — only enable when you need full SQL trace:
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true
#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.orm.jdbc.bind=TRACE
